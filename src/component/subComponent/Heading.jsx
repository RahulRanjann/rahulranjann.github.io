import React from 'react'

const Heading = (head) => {
  return (
    <div>
        <div className='title'>
            {head.title}
        </div>
    </div>
  )
}

export default Heading
