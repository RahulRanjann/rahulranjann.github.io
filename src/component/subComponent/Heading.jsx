import React from 'react'

const Heading = (head) => {
  return (
    <div className="w-full overflow-hidden">
        <div className='title'>
            {head.title}
        </div>
    </div>
  )
}

export default Heading
